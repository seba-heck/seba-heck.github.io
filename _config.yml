include:
  - projects
permalink: /:path/:basename
domain: seba-heck.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://seba-heck.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /_site/      # place folder name if the site is served in a subfolder

